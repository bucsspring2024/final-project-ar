import random
class Monster:
    def movement(monster):
        monster.jump
        
    def monster1(self, x, y):
        x = random.randint()
        y = random.randint()
        self.pos(x, y)
        
    def monster2(self, x, y):
        x = random.randint()
        y = random.randint()
        self.pos(x, y)
        
    def monster3(self, x, y):
        x = random.randint()
        y = random.randint()
        self.pos(x, y)
        
    def monster4(self, x, y):
        x = random.randint()
        y = random.randint()
        self.pos(x, y)